# URL del backend para el proxy de Vite.
# En Docker se pasa como variable de entorno (VITE_BACKEND_URL=http://backend:8080).
# Para desarrollo local sin Docker, copia esto a .env.development:
VITE_BACKEND_URL=http://localhost:8081
